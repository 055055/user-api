server:
  port: 8081
  servlet:
    encoding:
      charset: UTF-8
      force: true
      enabled: true

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    schema:
      classpath: data.sql
  h2:
    console:
      enabled: true
#      path: /h2-console